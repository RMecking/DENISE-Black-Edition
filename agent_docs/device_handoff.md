# Device Handoff / Rechnerwechsel

No active device handoff is recorded.

This file is intentionally transient. It must contain only the exact current
transport state while an explicit device handoff is active. Durable handoff
rules live in [`device_handoff_rules.md`](device_handoff_rules.md), and the
repository-wide operating model lives in the root [`AGENTS.md`](../AGENTS.md).

Do not infer the current task, branch, milestone, or publication authority from
historical versions of this file. When no handoff is active, reconstruct the
current state from Git/GitHub, the current task packet, the root `AGENTS.md`,
and the relevant canonical project documentation.
